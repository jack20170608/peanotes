# these are our own config values defined by the app
flyway {
  enabled=true
}

cranker-gateway = {
  enabled=false
  url="ws://localhost:3331"
}

# the database config
database {
  driver="org.postgresql.Driver"
  url="jdbc:postgresql://10.10.10.5:5432/peanotes"
  user="app_user"
  password=1
}


