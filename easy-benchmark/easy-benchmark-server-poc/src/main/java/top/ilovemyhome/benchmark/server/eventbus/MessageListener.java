package top.ilovemyhome.benchmark.server.eventbus;

import top.ilovemyhome.benchmark.server.application.AppContext;

public class MessageListener {

    public MessageListener(AppContext appContext) {
    }
}
